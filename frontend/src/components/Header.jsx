import React from "react";

function Header() {
    return (
        <header style={{ padding: 16, borderBottom: "1px solid #ccc" }}>
            <button>설정</button>
        </header>
    );
}

export default Header;